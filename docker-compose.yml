services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: ctf_harness
      POSTGRES_USER: ctf
      POSTGRES_PASSWORD: ctf
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  minio:
    image: minio/minio:RELEASE.2025-01-20T14-49-07Z
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: minio
      MINIO_ROOT_PASSWORD: minio123
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - minio_data:/data

  control_plane:
    build:
      context: .
      dockerfile: control_plane/Dockerfile
    environment:
      APP_ENV: dev
      DATABASE_URL: postgresql+psycopg://ctf:ctf@postgres:5432/ctf_harness
      MINIO_ENDPOINT: http://minio:9000
      MINIO_ACCESS_KEY: minio
      MINIO_SECRET_KEY: minio123
      MINIO_BUCKET: ctf-harness
      RUNS_DIR: /data/runs
      DOCKER_BIND_RUNS_DIR: /data/runs
      DOCKER_SOCKET: unix://var/run/docker.sock
      SANDBOX_IMAGE: ctf-agent-sandbox:latest
    ports:
      - "8000:8000"
    volumes:
      - ./runs:/data/runs
      - /var/run/docker.sock:/var/run/docker.sock
    depends_on:
      - postgres
      - minio

volumes:
  postgres_data:
  minio_data:
